# Uncomment the next line to define a global platform for your project
platform :ios, '12.4'
workspace 'Giituner'

target 'Giituner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Giituner
  pod 'AudioKit', '~> 4.0'

  target 'GiitunerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GiitunerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
